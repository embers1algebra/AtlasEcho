{
  "name": "atlas-echo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AtlasEcho – lightweight TypeScript project.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
